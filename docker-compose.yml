services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: lukalex-backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    restart: always
